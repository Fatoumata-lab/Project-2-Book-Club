<h1>All books</h1>

{{!-- <div class="cards">
    {{#each books}}
    <div class="card book" style="background-image: url({{cover}}">
        <a href="/book/{{this._id}}">
            {{this.title}}
        </a>
    </div>
    {{/each}}
</div> --}}