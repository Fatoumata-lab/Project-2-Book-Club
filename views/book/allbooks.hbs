<h1>All books</h1>

<div class="cards">
    {{#each books}}
    <div class="card-book" style="background-image: url({{cover}}">
        <a href="/books/{{this._id}}">
            {{this.title}}
        </a>
      <a href="/books/create"> Add book</a>
    </div>
    {{/each}}
</div>